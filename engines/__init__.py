"""Import engine modules for dispatcher compatibility."""

from engines.automation_engine import engine as automation_engine
from engines.claims_engine import engine as claims_engine
from engines.compliance_engine import engine as compliance_engine
from engines.construction_engine import engine as construction_engine
from engines.frequency_engine import engine as frequency_engine
from engines.geometry_engine import engine as geometry_engine
from engines.marketing_engine import engine as marketing_engine
from engines.publishing_engine import engine as publishing_engine
from engines.research_engine import engine as research_engine
from engines.writing_engine import engine as writing_engine
