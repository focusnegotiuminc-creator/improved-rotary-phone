[project]
name = "operator-os-agent-kernel"
version = "0.1.0"
description = "Reusable Operator OS toroidal-navigation agent kernel"
requires-python = ">=3.10"
dependencies = ["pyyaml>=6.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
