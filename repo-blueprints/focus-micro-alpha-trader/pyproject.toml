[project]
name = "focus-micro-alpha-trader"
version = "0.1.0"
description = "Paper-first micro-capital market research engine"
requires-python = ">=3.10"
dependencies = ["pyyaml>=6.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
