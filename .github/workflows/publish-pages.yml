name: GitHub Pages (Manual Optional)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pages-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Pages disabled for this repository plan
        run: |
          echo "GitHub Pages deployment is disabled in this repository by default."
          echo "Primary deployment path is Deploy TheFocusCorp Site workflow."

