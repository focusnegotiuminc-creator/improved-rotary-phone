name: Self Hosted Smoke Main

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: self-hosted
    steps:
      - name: Runner online
        run: echo "Self-hosted runner workflow reached job execution."
